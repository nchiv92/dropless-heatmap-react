import "./side-projects.scss";
import GuessingGame from "./guessing-game/guessing-game";

const SideProjects = (props: any) => {
  return (
    <div className="SideProjectContainer">
      <GuessingGame />
    </div>
  );
};

export default SideProjects;
