import { render, screen } from "@testing-library/react";

test.todo("correct component should be displayed when in using url - /");

test.todo("correct component should be displayed when in using url - /data");
